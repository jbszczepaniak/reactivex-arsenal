import { Observable } from 'rxjs/Observable';
import { TestScheduler } from 'rxjs/testing/TestScheduler';

describe('throttle', function() {
  // it('', function(assert) {
    // const ts = new TestScheduler(assert.deepEqual.bind(assert));
    // const up$ = ts.createHotObservable('--x----x--x---');
    // const down$ = ts.createHotObservable('--x-------x---');
    // ts.expectObservable();
    // https://blog.nrwl.io/rxjs-advanced-techniques-testing-race-conditions-using-rxjs-marbles-53e7e789fba5
  // });
});
